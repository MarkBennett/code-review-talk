{
  "tasks": {
    "files:create": "deno run --allow-env --allow-write --allow-read --unstable create-1000-files.ts",
    "files:update": "deno run --allow-env --allow-write --allow-read --unstable change-files.ts",
    "clean": "rm -rf files"
  }
}
